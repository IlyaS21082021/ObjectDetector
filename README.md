# ObjectDetector
Application to detect classes of objects on the image via yolo11n model. The app has client-server grpc architecture. Result is filterd by polygons. Input data are specified in yaml config
